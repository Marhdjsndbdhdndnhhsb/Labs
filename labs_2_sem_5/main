#Створення списку пар (число, його квадрат) за допомогою zip()
numbers = list(range(1, 6))  # [1, 2, 3, 4, 5]
squares = [num ** 2 for num in numbers]  # [1, 4, 9, 16, 25]
pairs_list = list(zip(numbers, squares))
print("1. Список пар (число, його квадрат):")
print(pairs_list)

#Використайте reduce() для підсумовування квадратів.
from functools import reduce
numbers = list(range(1, 6))
squares = [num ** 2 for num in numbers]

def add_function(a, b):
    """Функція, яка додає два числа."""
    return a + b
sum_of_squares = reduce(add_function, squares)

print("### Результат підсумовування reduce() ###")
print(f"Числа: {numbers}")
print(f"Квадрати: {squares}")
print(f"Сума квадратів: {sum_of_squares}")
